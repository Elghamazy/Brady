{
  "name": "brady",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js" // Optional: for development to auto-restart
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "spotify-web-api-node": "^5.0.2",
    "googleapis": "^105.0.0", // Add googleapis for YouTube API
    "morgan": "^1.10.0", // Optional: for logging requests
    "express-rate-limit": "^6.7.0" // Optional: for rate limiting
  },
  "devDependencies": {
    "nodemon": "^2.0.20" // Optional: for development to auto-restart the server
  }
}
